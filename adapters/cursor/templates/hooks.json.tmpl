{
  "version": 1,
  "hooks": {
    "preCompact": [
      {
        "command": "{{HOOKS_CMD_PATH}}/pre-compact.sh",
        "type": "command"
      }
    ],
    "sessionStart": [
      {
        "command": "{{HOOKS_CMD_PATH}}/post-compact.sh",
        "type": "command"
      }
    ]
  }
}
